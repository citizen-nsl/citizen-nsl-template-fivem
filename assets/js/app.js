const { createApp, ref, onMounted, reactive } = Vue;

createApp({
	setup() {
	  
	  
	  onMounted(() => {

		window.addEventListener("message", (event) => {

		});
  
	  });
  
	  return {
		// data
	  };
	},
  }).mount("#app");
  
